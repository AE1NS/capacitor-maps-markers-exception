import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-maps-markers-exception',
  webDir: 'dist/capacitor-maps-markers-exception',
  server: {
    androidScheme: 'https'
  }
};

export default config;
